Linear regression:
First, I could iterate some grid and create an array which is
$$
\sum^{\text{num points n}}\text{dist}(a_{i},a_{n})
$$
And then from there i just need to iterate different slopes and y intercepts
```
for slope m:
	for intercept b:
		distances: = get_points_on_line(grid, m, b)
		yield sum(distances)
```
And then I can find the min of the result.

Cool. that would take forever. Plus, I then have to find a way to map a line to a grid. For that I would  use some sort of round function...
![[meta/images/Pasted image 20250604160840.png]]
That means I can plug in an x and always get a round y