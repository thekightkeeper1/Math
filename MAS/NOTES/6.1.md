# Orthogonal
Two lines are perpendicular if and only if:
$$
\left| \left| u-v \right| \right| = \left| \left| u-(-v) \right| \right| 
$$
That is to say, whether your add or subtract the vectors their the result has the same length. this only is able to happen when
$$
u \cdot v = 0
$$


![[meta/images/Pasted image 20250324172215.png|225]]

# Length of a vector
The notation is
$$
\left| \left| v \right| \right| =\sqrt{ v \cdot v }
$$ 

# Normalization
This means to get the vector whose length is 1, and whose direction is the same as the original.
just do:
$$
\frac{v}{\left| \left| v \right| \right| }
$$

You can see how every vector is just $c$ times its base vector. So if we divide by that $c$, then we get it.

# Distance between vectors
Intuitively, the distance between 2 scalars is their difference, so do the same for vectors:
$$
\left| \left| u - v \right| \right| 
$$

# Orthogonal Complement
If $W$ is a space, then $W^\perp$ is the set that contains every vector $\perp$ to the $W$.
![[meta/images/Pasted image 20250327191132.png|275]]

# Finding $W^\perp$
Logically, if we want to get 0 as a result, we are going to use the nul space. After all, any vector in the null space times.